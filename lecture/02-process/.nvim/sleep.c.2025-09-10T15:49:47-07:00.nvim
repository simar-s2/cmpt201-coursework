#include <stdio.h>
#include <unistd.h>

int main() {
  int myNumber;
  printf("How many seconds do you want to run sleep for:");
  scanf("%d", &myNumber);
  printf("%d", myNumber);
  for (int i = 0; i < myNumber; i++) {
    sleep(myNumber);
  }
}
